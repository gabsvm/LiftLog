export const gainsLabRadii = {
  hero: 20,
  card: 16,
  compact: 12,
  control: 12,
  focus: 20,
  pill: 999,
} as const;

export const gainsLabTouchTarget = {
  minimum: 48,
  primaryAction: 52,
} as const;

export const gainsLabMotion = {
  pressMs: 110,
  setCompletionMs: 140,
  progressMs: 200,
  modalMs: 240,
} as const;
